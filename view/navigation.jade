nav(class="navbar navbar-default")
    div(class=container-fluid)
        div(class=navbar-header)
            a(class=navbar-brand)= $siteName
            if $user_name
              p(class="navbar-text navbar-right")= $user_name
            else
              button(type=submit,class="btn btn-default",onClick="window.location = '/auth/google/login';") Sign In
